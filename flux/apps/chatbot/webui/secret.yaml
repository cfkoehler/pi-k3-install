apiVersion: v1
kind: Secret
metadata:
  name: chatbot-router-credentials
  namespace: chatbot
  labels:
    managed-by: flux
stringData:
  # TODO(connor): set the real bearer token for the Mac-hosted router
  ROUTER_API_KEY: "REPLACE_ME"
